// export function autoGrow(textAreaRef: HTMLElement) {
//     const { current } = textAreaRef;
//     current.style.height = "auto"; // Reset the height
//     current.style.height = textAreaRef.current.scrollHeight + "px"; // Set the new height
// }